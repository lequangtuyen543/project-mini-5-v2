{
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT --watch database.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
